{
  "functions": {
    "source": "functions",
    "runtime": "nodejs20",
    "predeploy": ["npm --prefix functions ci", "npm --prefix functions run build"]
  },
  "firestore": {
    "rules": "firestore.rules"
  }
}
